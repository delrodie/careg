{% extends 'base.html.twig' %}

{% block title %}Activite{% endblock %}

{% block body %}
    <h1>Activite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activite.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ activite.type }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ activite.nom }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ activite.dateDebut }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ activite.dateFin }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ activite.lieu }}</td>
            </tr>
            <tr>
                <th>Situation</th>
                <td>{{ activite.situation }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ activite.description }}</td>
            </tr>
            <tr>
                <th>Latittude</th>
                <td>{{ activite.latittude }}</td>
            </tr>
            <tr>
                <th>Longetude</th>
                <td>{{ activite.longetude }}</td>
            </tr>
            <tr>
                <th>NomProprietaire</th>
                <td>{{ activite.nomProprietaire }}</td>
            </tr>
            <tr>
                <th>FonctionProprietaire</th>
                <td>{{ activite.fonctionProprietaire }}</td>
            </tr>
            <tr>
                <th>ContactProprietaire</th>
                <td>{{ activite.contactProprietaire }}</td>
            </tr>
            <tr>
                <th>Autorisation</th>
                <td>{{ activite.autorisation }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ activite.niveau }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ activite.createdAt ? activite.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ activite.updatedAt ? activite.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ activite.annee }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backend_activite_index') }}">back to list</a>

    <a href="{{ path('backend_activite_edit', {'id': activite.id}) }}">edit</a>

    {{ include('activite/_delete_form.html.twig') }}
{% endblock %}

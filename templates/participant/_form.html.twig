{{ form_start(form) }}
    <div class="form-group row">
        <div class="col-md-3 mt-4 text-right">{{ form_label(form.matricule) }}</div>
        <div class="col-md-6 mt-4">{{ form_widget(form.matricule) }}</div>
        <div class="col-md-2">
            {{ form_widget(form) }}
            <button class="btn btn-primary mt-4">{{ button_label|default('Ajouter') }}</button>
        </div>
    </div>

{{ form_end(form) }}

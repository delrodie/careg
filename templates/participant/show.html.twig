{% extends 'base.html.twig' %}

{% block title %}Participant{% endblock %}

{% block body %}
    <h1>Participant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participant.id }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ participant.matricule }}</td>
            </tr>
            <tr>
                <th>Carte</th>
                <td>{{ participant.carte }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ participant.nom }}</td>
            </tr>
            <tr>
                <th>Prenoms</th>
                <td>{{ participant.prenoms }}</td>
            </tr>
            <tr>
                <th>DateNaissance</th>
                <td>{{ participant.dateNaissance }}</td>
            </tr>
            <tr>
                <th>LieuNaissance</th>
                <td>{{ participant.lieuNaissance }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ participant.sexe }}</td>
            </tr>
            <tr>
                <th>Branche</th>
                <td>{{ participant.branche }}</td>
            </tr>
            <tr>
                <th>Fonction</th>
                <td>{{ participant.fonction }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ participant.contact }}</td>
            </tr>
            <tr>
                <th>Urgance</th>
                <td>{{ participant.urgance }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ participant.statut }}</td>
            </tr>
            <tr>
                <th>CreatedBy</th>
                <td>{{ participant.createdBy }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('participant_index') }}">back to list</a>

    <a href="{{ path('participant_edit', {'id': participant.id}) }}">edit</a>

    {{ include('participant/_delete_form.html.twig') }}
{% endblock %}
